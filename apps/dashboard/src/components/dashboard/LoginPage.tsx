import { useState } from 'react';
import { Brain, Eye, EyeOff, FlaskConical, HeartPulse, LockKeyhole, Pill, Plus, Stethoscope } from 'lucide-react';
import { postAuthLogin } from '@manhaj/api-client/src/auth/auth';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';

export function LoginPage({ onLogin }: { onLogin: (token: string) => void }) {
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const [showPassword, setShowPassword] = useState(false);
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); setError(''); try { const response = await postAuthLogin({ email, password }); const token = (response.data as { accessToken?: string }).accessToken; if (!token) throw new Error('Login response did not contain an access token'); onLogin(token); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to sign in'); } finally { setLoading(false); } };
  return <div className="min-h-screen bg-[#f8fbfb] p-2 sm:p-4"><div className="grid min-h-[calc(100vh-2rem)] overflow-hidden rounded-xl border bg-white shadow-sm lg:grid-cols-[1.08fr_.92fr]"><div className="relative hidden overflow-hidden bg-[#f5faf9] lg:block"><div className="absolute inset-0 opacity-60" style={{ backgroundImage: 'radial-gradient(#cfe5e1 1px, transparent 1px)', backgroundSize: '26px 26px' }} /><div className="absolute left-1/2 top-1/2 grid size-[430px] -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full border border-[#d7ebe8]"><div className="grid size-[310px] place-items-center rounded-full border border-[#d7ebe8]"><div className="grid size-40 place-items-center rounded-[2.5rem] border border-[#b9dcd7] bg-white/80 text-[#007b74] shadow-sm"><HeartPulse size={82} strokeWidth={1.1} /></div></div></div><MedicalIcon className="left-[15%] top-[12%]" icon={<HeartPulse />} /><MedicalIcon className="right-[17%] top-[10%]" icon={<Pill />} /><MedicalIcon className="left-[14%] top-[45%]" icon={<Stethoscope />} /><MedicalIcon className="right-[13%] top-[40%]" icon={<Brain />} /><MedicalIcon className="bottom-[16%] left-[26%]" icon={<FlaskConical />} /><MedicalIcon className="bottom-[12%] right-[24%]" icon={<Plus />} /><div className="absolute bottom-12 left-12 max-w-sm"><p className="text-xs font-bold uppercase tracking-[.22em] text-teal-700">Medical education, organized</p><h2 className="mt-3 text-3xl font-bold tracking-tight text-slate-800">Curate knowledge.<br /><span className="text-teal-700">Elevate learning.</span></h2><p className="mt-4 max-w-xs text-sm leading-6 text-slate-500">A focused workspace for building clear, reliable learning content for every student.</p></div></div><div className="flex items-center justify-center px-6 py-12 sm:px-12"><div className="w-full max-w-sm"><div className="mb-8 text-center"><div className="mx-auto grid size-16 place-items-center rounded-2xl bg-teal-700 text-white shadow-lg shadow-teal-700/20"><Plus size={32} strokeWidth={2.5} /></div><h1 className="mt-4 text-2xl font-bold tracking-tight text-teal-800">MedEd <span className="font-normal">Curator</span></h1><p className="mt-2 text-sm text-slate-500">Medical Education Content Management</p><p className="mt-5 text-sm text-slate-600">Sign in to your account</p></div><button type="button" className="flex h-11 w-full items-center justify-center gap-3 rounded-md border border-slate-200 bg-white text-sm font-medium text-slate-700 transition hover:bg-slate-50" onClick={() => setError('Google sign-in is not configured yet')}><span className="text-base font-bold text-[#4285f4]">G</span>Sign in with Google</button><div className="my-6 flex items-center gap-3 text-xs text-slate-400"><span className="h-px flex-1 bg-slate-200" />or<span className="h-px flex-1 bg-slate-200" /></div><form className="space-y-5" onSubmit={submit}><label className="block text-sm font-medium text-slate-700">Email<Input className="mt-2 h-11 bg-white" type="email" placeholder="you@example.com" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label className="block text-sm font-medium text-slate-700">Password<div className="relative mt-2"><Input className="h-11 bg-white pr-11" type={showPassword ? 'text' : 'password'} placeholder="Enter your password" value={password} onChange={(event) => setPassword(event.target.value)} required /><button type="button" className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400" onClick={() => setShowPassword((visible) => !visible)} aria-label={showPassword ? 'Hide password' : 'Show password'}>{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></label><div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2 text-slate-500"><input type="checkbox" className="accent-teal-700" />Remember me</label><button type="button" className="font-semibold text-teal-700 hover:underline" onClick={() => setError('Password reset is not configured yet')}>Forgot password?</button></div>{error && <p className="rounded-md bg-red-50 px-3 py-2 text-sm text-red-600">{error}</p>}<Button className="h-11 w-full bg-teal-700 hover:bg-teal-800" disabled={loading}>{loading ? 'Signing in...' : 'Sign in'}</Button></form><div className="mt-8 flex items-center justify-center gap-2 text-xs text-slate-400"><LockKeyhole size={13} />Secure administrator access</div></div></div></div></div>;
}

function MedicalIcon({ className, icon }: { className: string; icon: React.ReactNode }) { return <span className={`absolute grid size-14 place-items-center rounded-2xl border border-[#cfe5e1] bg-white/70 text-[#a8ceca] shadow-sm ${className}`}>{icon}</span>; }
